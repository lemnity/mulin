import { Caveat } from "next/font/google";

/** Рукописный шрифт для пометок на полях («Полезное каждую неделю» и т. п.). */
export const handwriting = Caveat({ subsets: ["cyrillic"], weight: "600", display: "swap" });
